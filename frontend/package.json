{
  "name": "turborepo-app",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "test": "next test"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "husky": "^8.0.3"
  },
  "packageManager": "yarn@1.22.19"
}
